version: 2
updates:
  # Keep the SHA-pinned workflow actions current; without this, pins silently
  # go stale and stop receiving upstream security fixes. Dependabot bumps the
  # SHA and the trailing version comment together.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
