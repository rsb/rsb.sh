---
import Base from "../../layouts/Base.astro";

// Stub. Becomes an Astro content collection (MDX, `lastUpdated`, typed contract
// references) per launch-artifacts §3 — laid out here so that lands without
// rework.
export const prerender = true;
---

<Base title="Architecture — rsb.sh">
  <h1 class="text-3xl font-semibold tracking-tight">Architecture</h1>
  <p class="mt-4 text-neutral-600">Not yet published.</p>
</Base>
